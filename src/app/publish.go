package app

import (
	"net/http"
	"../common"
	//"../common/log"
	//"../model"
	//
	//"fmt"
)


func Publish(sess *common.Session, w http.ResponseWriter, r *http.Request){
	r.ParseForm()
	//icon := r.FormValue("icon")
}